import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import {ScreenProps} from '../../navigation';

export default function JobDetail({navigation}: ScreenProps<'JobDetail'>) {
  return (
    <View>
      <Text>JobDetail</Text>
    </View>
  );
}
